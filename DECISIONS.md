# Decisions Log

ADR-lite log for this project. English. Append new decisions at the top, oldest at the bottom (matches `dwg7/chukei`'s and `dwg7/staccato-ecosystem`'s convention).

This is an internal working log, not a polished external communication — its wording is not necessarily vetted for wide sharing. It records findings about FERSPAS (including gaps or quirks in FAO's own data and infrastructure) in the same direct, working-notes register as everything else here. Before quoting or sharing any of it with FAO or another outside audience, rephrase with the same care this repo's README and CLAUDE.md already show, rather than passing this log along verbatim.

---

### D20 — Spec-compliant Map Intent support built and verified; D18's "identical scores" mystery partially resolved
**Date**: 2026-09-03
**Status**: `docs/map_intent.js` implemented per D19's reconciliation plan and confirmed working end-to-end against a real, hand-written, spec-compliant YAML intent.

**Built**: a Map Intent (YAML) paste-box (`📄 Map Intent` button, top-right toolbar — consolidated with the story-share button into one `#top-right-toolbar` row, fixing a layout collision found in the process) plus an optional URL-fragment path (`#intent=...`, compressed the same way as `story.js`, kept as the spec's explicitly-optional enhancement, not the required baseline). Parsing is defensive against `required_layers`/`optional_layers` items being either `{source_id, label}` objects or plain ID strings (D19's noted disagreement between the two normative spec documents). `render_hints.initial_center`/`initial_zoom` (and the schema's `center`/`zoom` naming) both map onto a single `map.flyTo()` — deliberately not the multi-step `story.js` mechanism, keeping Map Intent and this repo's narrative extension as two distinct, non-conflated concepts per D19.

**Verified against a real, hand-written sample intent** (cassava score + FAO's selected cassava site, DR Congo, `stars-martin` catalog context pointing at the real production `stars.optgeo.org`): parsed without error, correctly flew to `initial_center`/`initial_zoom`, correctly activated both `required_layers` entries (one raster, one vector — exercising both code paths), and displayed the `goal` field.

**D18's open question, partially resolved**: this same test surfaced clearly *non-uniform* scores at a different DR Congo location (Maize 56, Livestock 44, others 36) — a real, varied spread, unlike the earlier "six commodities all identical" observation. This weakens hypothesis (b) from D18 (a color-matching artifact collapsing values into shared buckets) relative to hypothesis (a) (genuine regional flatness at the specific earlier probe points) — not proof either way, but a data point worth recording rather than re-deriving later.

**Still to do from D19's plan**: 1–2 more sample intents covering different content shapes (a GAEZ-only interpretation query; a broader multi-layer comparison) before considering the "hand-built samples" step done; the actual ferspas57-specific Staff system prompt (following `staff-system-prompt.md`'s template, with our real 23-source catalog injected) hasn't been drafted yet; the narrative-extension ADR for `staccato-spec-19` is still pending on that.

---

### D19 — This session's `story.json` diverges from `staccato-spec`'s actual Map Intent format in two real ways; reconciliation plan
**Date**: 2026-09-03
**Status**: Found while starting the "Map Intent / Staff design" work item from D18's autonomous plan. Fetched the actual normative documents (`map-intent-vnext.md`, `staff-system-prompt.md`) directly from `UNopenGIS/staccato-spec` rather than continuing to build against an assumed shape.

**What the real spec says, that this session's prototype doesn't match**:
1. **Format: YAML, not JSON.** `map-intent-vnext.md` §2 normative requirement 1: "Map Intent MUST be serializable as YAML." This session's `story.json`/`SAMPLE_STORY` has been plain JS objects serialized as JSON throughout D15–D18.
2. **Sharing: plain text is the REQUIRED baseline, URL is explicitly discouraged for "faceless" deployments.** §2 requirement 3: "Map Intent MUST be shareable as plain text independent of URL state." §6 validation rule 5: "`sharing_policy.url_share` SHOULD be `false` in faceless Cartographer deployments" — and D17 already independently concluded this Cartographer should trend "buttonless"/faceless. This session's entire D15 mechanism (compress into a URL fragment) is the sharing mode the spec calls *optional* (`sharing_policy.url_share`), not the one it makes mandatory. hfu's original instinct, voiced before D15's URL-compression design — "改めてJSON貼り付けか?" (is it back to pasting JSON?) — turns out to have been closer to the spec's actual intended baseline than the URL-fragment mechanism this session built instead of it.
3. **`render_hints` is a single camera state, not a sequence.** The schema has one `center`/`zoom`/`bearing`/`pitch`, matching one map view — there is no `steps[]` array anywhere in the normative schema. A multi-step animated narrative (D15's whole premise) has no home in Map Intent v2 as currently specified. This is the concrete evidence for the spec gap `staccato-spec-19` already flagged in D12 as needing "a dedicated ADR once your narrating-Staff design has concrete shape" — it now has concrete shape, so that ADR is due, not deferred further.
4. **Minor, worth noting**: the two spec documents disagree with each other on `required_layers`' item shape — `map-intent-vnext.md`'s schema shows objects (`{source_id, label}`), `staff-system-prompt.md`'s worked example shows plain ID strings. Not this session's inconsistency to fix, but worth defending against both shapes when parsing rather than assuming one.

**Reconciliation plan**:
- Implement actual spec-compliant Map Intent support in the Cartographer: accept YAML (a `js-yaml`-style parse, still zero backend), support both a **paste-box** (satisfying the REQUIRED plain-text-sharing rule, and — notably — this is exactly the "back to pasting JSON" option hfu floated and this session talked past in D15) and the existing URL-fragment path (kept as the spec's explicitly-*optional* enhancement, not replaced) — the paste-box was hasty to treat as a fallback-of-last-resort; the spec suggests otherwise.
- Draft a real, ferspas57-specific Staff system prompt following `staff-system-prompt.md`'s template, with our actual 23-source catalog injected as startup config.
- Build 2–3 hand-written, genuinely spec-compliant (single-camera-state, YAML) sample Map Intents first, and confirm the Cartographer renders them correctly, *before* touching the narrative/multi-step question.
- Only then draft the narrative-extension ADR for `staccato-spec-19` (an optional, backward-compatible `narrative.steps[]`-shaped field, consistent with §7's "consumers that do not support new fields SHOULD ignore unknown keys" compatibility rule) — this is the concrete deliverable `staccato-spec-19` asked to be looped in on once there was real shape to review, and there now is.

---

### D18 — GitHub Pages live; AEZ57's 57-class legend sourced and implemented; autonomous work plan for hfu's absence
**Date**: 2026-09-03
**Status**: Pages live and confirmed. AEZ57 legend working, with one documented inference (not full primary-source certainty, unlike AEZ33). Autonomous plan set, execution starting.

**GitHub Pages published**: hfu approved pushing `main` to `origin` and enabling Pages (`docs/` on `main`) — done. Live at `https://dwg7.github.io/ferspas57/`, confirmed 200 with correct content.

**AEZ57's 57-class legend, sourced and built**: found the primary source — FAO/IIASA's GAEZ v5 GitHub wiki (`github.com/un-fao/gaezv5.wiki`, page "10. Agro-ecological Zones classification"), cloned directly since the REST content API doesn't expose wiki pages. Classes 0 and 49–57 are **directly confirmed** against this source (same special-purpose categories as AEZ33's 25–33, renumbered: very-steep-terrain, severe-soil-limitations, irrigated, hydromorphic, desert/arid, permafrost, arctic, built-up, water). Classes 1–48 are FAO's documented combinatorial scheme — 8 Temperature Regime Classes × {semi-arid, sub-humid, humid} moisture × {no/slight, moderate} soil-terrain limitation = 48 — and the **class definitions are exact**, but the wiki's prose ("AEZ-01 to AEZ-06: combinations of TRC1 with M2-M4 and S3-S4") describes the grouping, not a literal enumeration table; this session inferred the within-group ordering (moisture varying slower than soil/terrain) rather than reading it off an explicit numbered list. Documented as a caveat directly in `docs/aez57_legend.js`, not just here — if this class table is ever found to mismatch FAO's own rendered legend, the ordering assumption is the first thing to suspect, not the underlying class definitions. Implemented as `docs/aez57_legend.js`, mirroring D16's AEZ33 mechanism (center-probe nearest-color match against the official STAC colormap), wired into `index.html`, and visually confirmed working against production tiles.

**Open, not yet explained**: the score-comparison panel (D17) keeps showing several different commodities with *exactly* the same value at one probe point (e.g. six commodities all at 35, or all at 37 a moment later after panning) — changed between probes, so it's not frozen/stuck, but the coincidence rate seems too high to be pure chance. Two live hypotheses, neither confirmed: (a) genuine HIH behavior — broad regional areas may share a common baseline component in the underlying multi-criteria score (D16) before local factors differentiate it; (b) an artifact of nearest-color matching against a 101-entry ramp, where real-but-close values collapse into the same bucket. Worth a focused check (e.g. reading several real Score COGs' raw float values at the same coordinates directly, bypassing the color round-trip) before trusting this feature's precision for anything narrative-critical.

**Autonomous work plan, hfu stepping away for ~8 hours**: agreed priority order —
1. Map Intent / Staff design: formalize the `story.json` shape as `staccato-spec`'s Map Intent concept, draft a Staff system prompt that produces one from a free-form user query referencing real layer IDs/coordinates, hand-build 2–3 sample intents across different content types (GAEZ interpretation, HIH country narrative, cross-layer comparison) and verify each plays back correctly.
2. Horizontal expansion: batch-convert the mergeable-across-country HIH themes (fish-farming across COD/CIV/CAF/CMR/COG, accessibility across COD/CIV/CAF/COG per D8) for the five countries left unscoped since D9, plus whatever per-country commodity data exists.
3. Fact-gathering: basemap (Positron/OpenMapTiles) language-field coverage against the 10-language target list; HIH background/country context for the five newly-added countries.
- Ground rule set by this session, not hfu explicitly but consistent with how this whole session has operated: new local conversion/research work proceeds freely and gets committed; anything that would touch shared production infrastructure again (new `stars.optgeo.org` uploads, new `hfu/stars` PRs) gets prepared and drafted but held for hfu's review before actually being requested/merged, rather than repeating D15's upload flow unsupervised at scale.

---

### D17 — Site moved into `docs/` against production URLs; a real Martin URL-format bug found; interaction/visual-design principles for a device-agnostic, buttonless Cartographer
**Date**: 2026-09-03
**Status**: `docs/` now holds the real client (not yet pushed/GitHub-Pages-enabled — see "Still open"). Several design principles set for how this site should look and behave going forward, mostly not fully executed yet.

**Migration to `docs/` against production**: per hfu's direction to develop against GitHub Pages rather than the local `:8123` test server, moved `index.html`/`story.js`/`aez_legend.js`/`score_probe.js` from this session's scratchpad into the repo's `docs/`. `TILE_SERVER` now points at `https://stars.optgeo.org`, vector `final` layers now fetch from `https://depot.optgeo.org`.
- **Real bug found while doing this**: Martin serves tiles at extension-less URLs (`/{source}/{z}/{x}/{y}`) and `301`-redirects a `.png`-suffixed request to the same path without it. This session's local testing had used `pmtiles serve` (the CLI's own proxy), which does expect `.png` — the two tools disagree on this convention, and the mismatch wasn't visible until testing against the real `stars.optgeo.org`. Fixed in both `index.html`'s raster `tiles` template and `score_probe.js`'s tile-fetch URL.
- **Basemap reference features were being buried, not just labels**: D13's "insert below the first `symbol` layer" fix only protected place-name labels; water bodies, waterways (rivers), roads, and administrative boundaries in Positron's style are `fill`/`line` layers that render *before* symbols, so they were still getting painted over by our raster. Fixed by inserting at the first `line`-type layer instead (in Positron's actual layer order, checked directly: base land-color fills — background/landcover/landuse/parks — come first, then the first `line` layer is `waterway`, with every water/boundary/road/building/label layer after that point). hfu's stated reason: DR Congo's rivers are sometimes very wide and make for good narrative anchors, and it's simply better cartography to keep reference geography legible under the data.

**A cursor-anchored "score comparison" feature, replacing plain choropleth overlays for HIH's Score data**: hfu's core observation — stacking filled-raster overlays makes every commodity's Score layer look "roughly the same," which undersells data whose actual interest is the *relative* difference between commodities at one place ("is this spot better for fish farming or cassava?"). Built `score_probe.js`: reads each of 13 relevant sources' own tile *independently* (fetched as a plain `Image` → 2D canvas → `getImageData`, not read off the rendered WebGL screen) so the comparison doesn't depend on which layer happens to be drawn on top or even be checked as visible; reverses each pixel's color back to an approximate 0–100 score via nearest-match against the same ramp used to build the tiles (D10); renders a sorted, live-updating bar list. Deliberately not over-engineered per hfu's own caution against premature optimization (packing multiple commodities' values into fewer combined-pixel layers was floated and explicitly set aside — simple per-source tiles for now).

**Interaction model: pan-to-sample from a fixed screen-center point, not free mouse hover** — a real design discussion, not just an implementation detail. hfu's reasoning: hovering has no equivalent on touch devices (no persistent pointer position without an active touch), while "pan the map to move the sample point under a fixed marker" is the same physical gesture on mouse and touch alike. Converted both the score-comparison probe and D16's AEZ33 dynamic legend to this model — sampling now happens on the map's `move` event, reading from `map.getCenter()`/the canvas's actual center pixel, not `mousemove`. Both features now share one on-screen marker rather than each drawing their own.
- **The marker's shape matters**: a first pass called it a "crosshair," but hfu flagged that a literal crosshair (a "+" mark) risks reading as a weapon-sight/targeting reticle — an unfortunate association for a site about a conflict-affected country. Changed to a plain circle/ring (which is what the visual already was — `border-radius: 50%` — but the code's own naming and comments still said "crosshair," so renamed throughout to "probe ring" to avoid the association even in code, not just on screen).
- Fixed a real bug found in the process: the score probe's original "only re-sample when the cursor crosses into a new tile" cache key meant it wouldn't update at all for sub-tile cursor movement — looked like several commodities coincidentally sharing identical scores (a red flag hfu caught: "why are six values exactly the same?"). Switched to re-sampling on every settled `move` event regardless of tile boundaries (tile image fetches are still cached per-tile, so this doesn't add redundant network requests).

**UI chrome — set to minimal/collapsed by default, with a stated direction toward "buttonless" entirely**: the layer-checkbox panel now starts collapsed (hfu: this is the correct default, not just an option). hfu's broader framing: since language, layer selection, and narrative content are all meant to arrive pre-baked in a Staff-generated link (see below), the Cartographer itself should trend toward having no visible menu chrome at all for that arrival flow — the checkbox panel is realistically a builder/explorer affordance, not what an end user following a shared narrative link needs to see. Also good for mobile screen space, independently.

**Narrative language is Staff's job, not the Cartographer's — confirmed and extended**: hfu's framing, building on D16: "the map page has no language menu, but arriving via Staff, it's already magically in your preferred language" — the elegance is specifically that the Cartographer needs *no* language-selection UI at all, because Staff (which already knows the user's language from the conversation) bakes one single language into the story JSON before handing over the link, rather than shipping all languages and switching client-side (which is what D16's prototype actually does — a demo convenience, not the intended production shape). Concretely: production stories should carry one language's `title`/`caption` strings, decided upstream of the Cartographer, not a `{en:..., fr:..., ...}` object for a client switcher to pick from. Fixed a related bug in the process: `index.html`'s story playback hadn't been updated for D16's new multi-language caption objects and would have rendered `[object Object]` — patched with `pickLang(field, "en")`, English hard-coded as the default for now per hfu ("English is fine to start").
- **Target language list extended**: added Italian — hfu's reasoning: FAO is headquartered in Rome, so Italian is a deliberate nod to the host country, not just UN-official-language coverage. Full target list is now UN's six (en/fr/es/ru/zh/ar) + de + ja + it + sw (D16's regional-challenge choice) = 10. Sample story translated into all 10.
- **Flagged for later, not solved now**: (1) the basemap's own place-name labels could in principle localize along with Staff's chosen language too (many OpenMapTiles-based styles, likely including Positron, carry `name:zh`/`name:ja`/etc. fields) — hfu noted the impact of this would be substantial specifically for Chinese; (2) that same idea immediately raises font/glyph concerns — CJK Han-unification means the same codepoint can render with subtly different (Chinese/Japanese/Korean-conventional) glyph shapes depending on font/`lang` handling, and Arabic's cursive shaping is its own separate, harder problem. None of this is implemented; noted so it isn't rediscovered from scratch later.

**Still open**: `docs/` has not yet been pushed to `origin`, and GitHub Pages hasn't been enabled in repo settings — hfu asked whether this could be published "quietly" (no announcement) and this session's answer was: technically straightforward, but wait until doing so doesn't show broken raster layers — which is now moot since D15's production PRs are merged and confirmed live (`stars-fd`, this session). Actually flipping Pages on is still a distinct, not-yet-taken step in the "publishing public content" sense and should get an explicit go/no-go before it happens, not be inferred from "the prep work is done."

---

### D16 — Multilingual narrative, a dynamic (mouseover) AEZ legend, and the real reason behind D13's mystery
**Date**: 2026-09-03
**Status**: Both prototyped and working. HIH background research also done, feeding directly into narrative content.

**HIH research (hfu's request — narrative source material)**: the Hand-in-Hand Geospatial Platform launched in 2020, led by FAO's Agro-informatics team; over 65 countries/institutions have taken part. Its actual mechanism is a "Territorial/Agricultural Typology": **"Suitability Assessments are based on GIS-driven multi-criteria evaluation"** combining agro-climatic suitability with accessibility and a poverty/priority component — not a pure agronomic score. **This directly and authoritatively resolves D13's open question**: the maize-storage site FAO actually selected sits in a GAEZ zone flagged as having severe soil/terrain limitations, yet scores higher than a textbook-favorable zone, because HIH's scoring is explicitly designed to weigh poverty-reduction potential (the Initiative's real mission — SDG1/SDG2) alongside raw climate suitability, not to find the agronomically "purest" spot. D13/D15's sample story's final caption has been rewritten to state this as a sourced explanation, not just an open question. On DR Congo specifically: vast land/water resources but constrained by conflict, disease outbreaks, currency depreciation, and rapid deforestation (92.1%→80.3% forest cover) from unmanaged mining/urban growth — real context for why FAO's own framing treats this as a youth-opportunity/development story, not just a data-collection exercise.

**Multilingual narrative — implemented**: hfu's framing was that this is a genuine AI strength worth exercising, referencing multilingual UN/broadcast contexts. Rebuilt the story schema so `title`/`caption` are objects keyed by language code rather than plain strings (`pickLang(field, lang)` falls back to English, or any available language, if backward-compat plain strings are ever passed). Translated the full 4-step maize/GAEZ story into the UN's six official languages (en/fr/es/ru/zh/ar) plus German and Japanese, plus **Swahili as the harder "challenge" language** — DRC's own official language is French, already inside the UN set, so it wasn't actually a stretch; Swahili is the language genuinely spoken in the story's real setting (eastern DRC, near the Rwanda/Uganda border) and was chosen for that reason, not arbitrarily. All translation was done by this session directly (no native-speaker review) — treat as a working demonstration of the mechanism, not publication-ready copy, especially for Arabic/Chinese/Russian/Swahili where this session's fluency is weakest.

**Dynamic (mouseover) legend for AEZ33 — implemented, with a real MapLibre v5 gotcha found along the way**: hfu's framing, referencing `kitavolca`'s approach — show the one relevant legend entry near the cursor, not a static list of all 33 classes sitting on screen. Mechanism: since our PNG8 tiles are baked from FAO's own official colormap (D8), the actual rendered pixel color can be read back off the WebGL canvas and reverse-matched to a class.
- First attempt used `preserveDrawingBuffer: true` on the `Map` constructor (the documented way to keep a WebGL buffer readable outside the render loop) — **this option is silently ignored in MapLibre GL JS 5.24.0**: confirmed via `gl.getContextAttributes()` on a freshly-created, otherwise-empty map that the resulting context always reports `preserveDrawingBuffer: false` regardless of the constructor option. Not a config mistake on this session's part — reproduced in isolation.
- Working fix: don't rely on the buffer persisting. On `mousemove`, call `map.triggerRepaint()` and read pixels *synchronously inside* the `render` event it triggers (`map.once("render", ...)`) — the frame is guaranteed valid at that exact moment, before the browser's next composite/clear. This is the same pattern well-known raster-picking implementations use for exactly this reason.
- Second gotcha: exact color matching (which works fine for flat-Mercator raster rendering with `raster-resampling: nearest`) isn't reliable once **globe projection** is active — globe reprojects the flat tile texture onto a sphere mesh, and that resampling doesn't obviously respect the raster-resampling paint property the same way, so baked palette colors can arrive slightly blended. Switched to nearest-color matching (smallest squared RGB distance, rejecting anything not reasonably close) instead of an exact lookup table — confirmed correct in testing (hovering the Congo basin correctly reported "AEZ33 — 26: Land with severe soil/terrain limitations", matching D13's own finding).
- Scoped to `gaez-aez33` only for now (its 33-class legend is verified against FAO's primary GAEZ v4 guide, D13); AEZ57's 57-class legend text hasn't been sourced yet.

---

### D15 — Production upload done (2 martin.yaml PRs open); narrating-Staff prototype: story JSON compressed into a URL fragment, played back client-side
**Date**: 2026-09-03
**Status**: Upload complete and verified. Narrative transport mechanism prototyped and working end-to-end, not yet connected to an actual Staff (the sample story is hand-written, not LLM-generated).

**Production upload — done**: hfu approved proceeding; `stars-fd` confirmed the earlier `spacex.optgeo.org` mention was not a different host — `spacex` is just the machine's SSH hostname, with Martin (`stars.optgeo.org`) and Caddy (`depot.optgeo.org`) as its two public-facing roles, "三位一体" (one machine, three names) per hfu. All 23 files transferred and checksum-verified. The 7 GeoJSON final-location files needed no config change and are already live via `depot.optgeo.org` (confirmed 200 + CORS). Opened two PRs against `hfu/stars` for the 16 PMTiles sources, matching the existing `config/martin.yaml` schema (`pmtiles.sources.<name>: /home/stars/data/<file>.pmtiles`) and `stars-fd`'s requested split:
- [`hfu/stars#9`](https://github.com/hfu/stars/pull/9) — `gaez-aez33`, `gaez-aez57`
- [`hfu/stars#10`](https://github.com/hfu/stars/pull/10) — the 14 HIH DR Congo sources
Both awaiting `stars-fd` review/merge + Martin restart as of this writing.

**Narrative implementation — the actual design problem, per hfu**: a single Staff→Cartographer handoff (one Map Intent) can't express an animated, multi-step narrative — you need something closer to a story map's sequence of scenes. hfu's own constraints going in: stay GET-only (no POST — a static Cartographer site, per this repo's whole architecture, has nowhere to receive one), and avoid needing a manual "paste the JSON in" fallback if avoidable.

**Solution prototyped and confirmed working**: a narrative is a small JSON script — `{title, steps: [{center, zoom, layers, caption}, ...]}` — compressed with `lz-string` (`compressToEncodedURIComponent`) and carried entirely in the URL **fragment** (`#story=...`), not a query string or POST body. The fragment is never transmitted to any server, so this fits a pure static-file Cartographer perfectly and, as a side benefit, never shows up in server/CDN access logs the way a query string could. Built the actual sample story from D13's real, verified maize/GAEZ finding (4 steps: GAEZ context → score overlay → the surprising final-site location → the open question) and confirmed:
- The 4-step story, Japanese captions included, compresses into a **1,321-character URL** — comfortably inside any practical browser URL-length limit, with real headroom for longer/richer stories.
- "Playing" a story: collapses the layer panel, syncs the checkbox state to each step's active layers, `map.flyTo()`s between steps (1.8s), and displays the caption — with prev/next/autoplay controls. Verified visually stepping through to the "surprising final site" step: the right layers, the right location (Nyamilima), the right caption, in sync.
- This is architecturally just a richer instance of what `README.md` already describes Staccato as doing ("a plain-language question into a real map link") — a narrative is a link that encodes a sequence of views instead of one, not a new mechanism requiring backend support.

**Not yet done**: no Staff actually generates these JSON scripts yet — the sample story was hand-written by this session as a proof of the transport/playback mechanism, using D13's already-verified real finding as content. Producing narratives from an LLM call (the actual "narrating Staff" from D6/D12) is separate, follow-on work.

---

### D14 — All 23 files now genuinely complete (fish-farming was missed, then a real type error found in one of them); checkbox-based multi-layer UI working
**Date**: 2026-09-03
**Status**: Corrected and complete. Local client now supports combining layers, not just viewing one at a time.

**D13's "all 23 files exist" claim was wrong** — the 4 fish-farming files (`hih-fishfarm-closed`, `-open`, `-extensive`, `-closed-final`) were missing entirely; the batch script only covered accessibility + 7 commodities. Caught while wiring up the client UI (files referenced by the UI that didn't actually exist on disk). Built them the same day, same pipeline.

**A real data-shape error found in the process, not just an omission**: `hih-fishfarm-closed-final` (source: `COD-FISH-FIN-CLOSED`) was built with D10's binary-site vector pipeline (`numpy.where(A==1, 1, 0)` → polygonize) by blind analogy to the commodity `FINALLOCATION` layers — and produced **0 features**. Checking the actual raster (which the process should have done before building, per D10's own stated lesson) showed why: it's Float64, range 28.19–83.77 — a **continuous score, not a binary site flag** — despite "FIN"/"final" in its name suggesting the same shape as `COD-FINALLOCATION-MAIZE`. Not every FAO layer sharing a naming pattern shares a data shape; `FIN` here apparently means something closer to "a later/refined round of scoring," not "the selected site." Rebuilt with the Score pipeline instead (PNG8, 0–100 domain) — 21.9KB, tiny because the underlying data footprint is small, not because anything is wrong. The other 6 commodities' `FINALLOCATION` files were re-checked against this and are fine (each produced a plausible, non-zero, varied polygon count — the telltale sign of genuine binary data, unlike fishfarm's silent zero).

All 23 files (D9's list) now genuinely exist and are internally consistent: 16 raster PMTiles (2 GAEZ + 3 accessibility + 4 fish-farming + 7 commodity scores) and 7 vector GeoJSON (commodity final-locations only, fish-farming's own "final" turned out not to be one of these).

**Client UI — checkboxes, not a dropdown**: rebuilt the local test page around hfu's framing — the interesting thing isn't any one layer alone, it's seeing how layers relate, so the interface should let a User freely combine them (Staccato-style composability) rather than force a one-at-a-time selection. Implemented as grouped checkboxes (GAEZ / crop-storage scores / accessibility / fish-farming / final sites), each toggling a source+layer pair independently; raster layers still insert below the basemap's label layer (D13) and fade in over one zoom level at their minzoom (D12). Verified by combining `hih-cod-maize-score` with `hih-cod-maize-final`: the selected maize-storage site renders correctly on top of the suitability gradient, right next to a real named settlement (Nyamilima) on the Positron basemap — a small, concrete confirmation that the siting data lines up with real-world geography in an intuitively sensible way, not just internally consistent numbers.

**Process, now underway**: hfu approved moving forward with the actual `stars.optgeo.org` upload and `config/martin.yaml` PR(s). Asked `stars-fd` (2026-09-03) two concrete questions before proceeding: how to physically get these files onto the Pi from this session's own filesystem, and whether the 7 tiny GeoJSON files should be served as plain static files (via `depot.optgeo.org`'s Caddy) rather than tiled through Martin, given their size (5–70KB, a handful of polygons each) doesn't warrant real vector-tiling. Reply pending as of this writing; not blocking further client-side work.

### D13 — Full DR Congo batch conversion complete; client-map fixes (globe, label-over-raster ordering); a verified real narrative example
**Date**: 2026-09-03
**Status**: All 23 D9-scoped files now exist locally (not uploaded anywhere). Client test page reflects D11/D12's design decisions, two real implementation bugs found and fixed along the way.

**Batch conversion (D11's approval) — complete**: all remaining 12 HIH commodity files (score PMTiles + final-location GeoJSON for cassava, cocoa, coffee, palm-oil, wheat, livestock) and 3 accessibility PMTiles (`hih-access-urban`, `hih-access-urban-weighted`, `hih-access-port`) built via a single script reusing D10/D11's validated pipeline. All succeeded on the first run. Final-location feature counts varied meaningfully by commodity (12–67 polygons, vs. maize's 3) — confirms D10's sparse-vector call was right in kind, and that "sparse" varies a lot by commodity, not a fixed small number. Combined with D8's two GAEZ files, all **23 files from D9's naming list now exist**, matching that list exactly.

**Two real client-side bugs found and fixed while implementing D11/D12's design decisions**:
1. **Duplicate attribution control** — this session added its own `AttributionControl` without disabling MapLibre's default one (`attributionControl` defaults to `true` in the Map constructor), producing two overlapping attribution bars. Fixed with `attributionControl: false` in the constructor alongside the explicit control.
2. **Globe projection silently not applying** — setting `projection: {type: "globe"}` in the `Map` constructor had no effect (`map.getProjection()` returned `undefined`/mercator) because Positron's fetched `style.json` declares its own top-level `projection` field, which overrides the constructor option once that style loads. Fix: set `projection` directly on the fetched style object (`base.projection = {type: "globe"}`) before passing it to `Map`/`setStyle` — confirmed working via `getProjection()` returning `{"type":"globe"}` after the fix. Relevant to anyone doing cafebabe's "runtime-hydration" style pattern (D6) with globe projection: the constructor-level option alone isn't reliable against a fetched third-party style.

**Cartographic principle, hfu — layer ordering matters, not just which basemap**: the data raster must be inserted *below* the basemap's label/road/border (`symbol`-type) layers, not appended on top of the whole style (which is the naive default when you `push()` onto a fetched style's layer array — MapLibre paints layers in array order, later = on top). Appending naively meant the raster buried all of Positron's place names inside the study area while leaving basemap labels cluttering everywhere *outside* it — a map that's quiet exactly where the data is and noisy exactly where it isn't. hfu's framing: "necessary data exists inside the study area too, unnecessary data exists nowhere — that's cartography." Fixed by inserting the raster layer at the index of the basemap's first `symbol`-type layer (`base.layers.findIndex(l => l.type === "symbol")`) rather than at the end. Confirmed visually: place labels (city names, country names) now render on top of the maize-score raster as expected.

**A real, verified narrative example — not hypothetical**: sampled the actual pixel values at the 3 real `hih-cod-maize-final` site locations against both `COD-LOCATIONSCORE-MAIZE` and `AEZ33-1KM` directly (`gdallocationinfo -wgs84`). Site 1 (≈29.44°E, 0.50°N) sits in GAEZ class 3 ("Tropics, lowland; humid" — a textbook-favorable zone for maize) with a maize score of 57.4. Sites 2 and 3 (≈29.57–29.58°E, ≈-0.87 to -0.88°N) sit in GAEZ class 26 — verified against the primary source (`GAEZ v4 Data Repository User's Guide` PDF, not just a web-search summary, which had actually mis-numbered this: the guide gives "25. Dominantly very steep terrain; 26. Land with severe soil/terrain limitations; 27. Land with ample irrigated soils; 28. Dominantly hydromorphic soil") — with higher maize scores, 65.3–65.5. The real tension: the "textbook favorable" site scored *lower* than two sites flagged with a soil/terrain limitation. This is genuine, verified, data-backed material for the D4 "narrating Staff" concept — a real question to surface (why did FAO's own composite score favor land with a stated limitation over a classically favorable zone — logistics/accessibility factors outweighing raw agro-climatic suitability, perhaps?), not an answer to assert.
- Also found (WebSearch): FAO has documented real Hand-in-Hand use of cassava-storage-siting-style geospatial analysis to guide actual cassava flour production investment in "Congo," aimed at reducing food-import dependence and improving nutrition — genuine real-world grounding for why `hih-cod-cassava-*` specifically matters, not just a data curiosity. **Caveat, not yet resolved**: search results inconsistently referred to "DRC" vs. "Republic of the Congo" (COG — a different country, also in D6's six-country shortlist) for this specific story; needs checking against the primary FAO source (a linked PDF) before being used confidently in any real narrative, to avoid attributing the story to the wrong country.

### D12 — Accessibility layer data shape, a UX polish (zoom-fade), a deferred idea (terrain), peer replies on AIDA credit and the Staff-narration spec gap
**Date**: 2026-09-03
**Status**: Decided/recorded, feeding directly into the batch conversion now in progress.

**Accessibility layers can reuse the Score pipeline as-is**: `COD-ACCESS-URB` inspected directly — Float64 (not Float32 like Score), but already a clean 0–100 range (`Minimum=0.000, Maximum=100.000` exactly) with only the standard declared NoData, no `-9999`-style hidden sentinel spotted (unlike D10's Score finding). Since it's already naturally scaled 0–100, the same fixed-domain, sequential-ramp, PNG8-indexed pipeline from D10/D11 applies directly to `hih-access-urban`/`hih-access-urban-weighted`/`hih-access-port` — no new domain-fitting logic needed. (Each new file should still get its own histogram check before assuming this holds, per D10's own lesson about not generalizing from one sample.)

**UX polish, hfu**: at the minzoom boundary of any layer, fade opacity in/out over roughly one zoom level (a MapLibre `raster-opacity` `interpolate`-on-zoom expression) rather than having data hard-cut in/out — "spo̜ing" out of existence at the exact minzoom is jarring, fading in over the zoom right below it isn't, and hfu framed this as characteristically dwg7 attention to visual detail. Not yet implemented (client-map work is still pending per D11).

**Deferred, not urgent**: hfu raised, for later consideration only, whether to layer in terrain/hillshade display via `mapterhorn-japan-bridge`'s work. No decision needed now.

**Peer replies, both worth keeping**:
- `staccato-ecosystem` (re: D4's AIDA finding) — pushed the reciprocity framing one step further than this session had: rather than merely avoiding a false novelty claim, *actively credit* AIDA's "from where is the data to what do I need to know" framing as having shaped this project's thinking, whenever this becomes externally visible (the Sept 28 FAO CSI talk, or direct correspondence) — the same warmer, credit-generously spirit `CLAUDE.md`'s "Positive intent" section already asks for regarding FERSPAS's STAC catalog itself. Also declined to fold either the AIDA finding or the cross-session-consulting pattern into its own `methodology/` yet (single data point; its own quality-checklist already warns against generalizing off one instance) — and redirected the cross-session-consulting question to `cafebabe` as the better-scoped home if it ever recurs enough to be worth documenting, rather than `staccato-ecosystem` (whose methodology is about Staff-role planning for a User, not inter-session coordination).
- `staccato-spec-19` (re: D6's "narrating vs. listening Staff" distinction) — confirmed this is a genuine, undocumented spec gap, not just an unstated detail: `architecture-principles.md` §5.2 gives Staff only a reactive "interpret user requests" action, and principle 1 ("User owns intent") implicitly assumes User-originated questions throughout. A proactively-narrating Staff originates intent itself, a real deviation from principle 1 as worded — separate from principle 2 (human-mediated handoff), which still applies regardless of who originates the intent. Proposed the load-bearing question for a future ADR: what actually triggers a narration, and whether principle 1 should be amended to something like "User owns intent, or ratifies Staff-originated intent before handoff." Offered to help draft/review that ADR once ferspas57's narrating-Staff design has concrete shape — deliberately not before, matching how ADR 0007/0008 got proposed there. This session agreed to hold off and take up the offer later.

### D11 — First real conversions verified end-to-end; batch scope approved; client-map decisions (globe projection, Positron basemap, attribution, minimizable UI)
**Date**: 2026-09-03
**Status**: Decided. GAEZ (both layers) and HIH `COD-*-MAIZE` (score raster + final-location vector) built, verified, and visually confirmed in a local MapLibre test. Remaining 12 HIH commodity files + 3 accessibility files approved to batch-convert with the same pipeline.

**What's actually built and verified so far** (local only, nothing uploaded anywhere yet):
- `gaez-aez33.pmtiles` (34.3MB), `gaez-aez57.pmtiles` (41.4MB) — D8's validated pipeline (official FAO colormap → GDAL color-table VRT → MBTiles with `RESAMPLING=NEAREST` at *every* step → `gdaladdo -r nearest` pyramid → `pmtiles convert`), `pmtiles verify` clean on both.
- `hih-cod-maize-score.pmtiles` (1.3MB, z4–z8) — D10's Score pipeline (mask `-9999`+declared nodata via `gdal_calc.py`, round/clip to a fixed 0–100 domain, bake the session's own sequential ramp via the same color-table-VRT technique, same MBTiles/pyramid/convert steps). Loaded into MapLibre locally and visually confirmed: a smooth suitability gradient over most of the area, with a cleanly-demarcated excluded zone rendering as intended transparency (not a bug — the `-9999` mask showing through to the background).
- `hih-cod-maize-final.geojson` (5.4KB, 3 polygon features) — D10's vector approach for FinalLocation: `gdal_calc.py` to isolate value==1, `gdal_polygonize.py` to extract real site geometries (not reduced to centroids — the actual ~1km-resolution site footprints are already compact and informative on their own), enriched with `country`/`commodity`/`source_collection`/`site_id` properties.
- **Approved**: apply this same two-track pipeline (Score → PNG8 raster PMTiles, FinalLocation → GeoJSON polygons) to the remaining 6 DR Congo commodities (cassava, cocoa, coffee, palm-oil, wheat, livestock) and the 3 accessibility layers (`hih-access-urban`, `hih-access-urban-weighted`, `hih-access-port`) from D9's naming list.

**Client-map decisions, all hfu, same session**:
- **Globe projection, not flat Web Mercator** — reasoning: Togo, backed by the full African Union (55 member states), is bringing a resolution to the UN General Assembly in September 2026 specifically demanding institutions move away from the Mercator projection because it visually shrinks Africa relative to its true size (Mercator makes Greenland look Africa-sized; Africa is actually ~14× larger). Verified this is real and current (WebSearch, 2026-09-03) — Togo's Foreign Minister confirmed the resolution is ready for a UNGA vote this September, the AU has already adopted its own resolution favoring the Equal Earth projection. Given this repo's HIH content is specifically about African nations (DR Congo, Central African Republic, Cameroon, Republic of Congo, Côte d'Ivoire), using MapLibre's globe projection (zero area distortion, arguably a stronger fix than even Equal Earth's flat compromise) rather than flat Mercator is directly responsive to this — not a cosmetic choice.
- **Basemap: Positron** (CartoDB's light, low-saturation, minimal style) as the default underlying vector basemap — chosen so it doesn't visually compete with the thematic overlay colors (the sequential score ramps, GAEZ's classification palette). Where/how to actually splice this in (cafebabe's D6 "runtime hydration" pattern, sourced from `stars.optgeo.org` vs. elsewhere) is still to be worked out, not yet implemented.
- **Embed proper `attribution` in every PMTiles archive's metadata** (not left to be wired up separately client-side) — MapLibre picks this up automatically (cafebabe, D6), and doing it at the data layer rather than the app layer makes the attribution control (bottom-right, in MapLibre's default UI position) correct "for free" regardless of which map/site ends up consuming a given file. Not yet done for the files built so far — needs to be added retroactively to `gaez-aez33`/`gaez-aez57`/`hih-cod-maize-score` and included from the start for the batch.
- **The info/legend panel (top-left in current tests) should be collapsible/minimizable** — flagged as good UI practice, not yet implemented in the local test page.

**Still to think about, no rush**: what vector-tile basemap serving/splicing approach to actually use (per cafebabe's runtime-hydration pattern) — hfu asked this be picked up "when there's a free moment," not blocking current conversion work.

**Process**: per hfu, once the `.md` files are updated (this entry), send a short progress update to the `staccato-ecosystem` and `staccato-spec` peer sessions — not asking them for anything specific right now, just keeping them informed per `CLAUDE.md`'s knowledge-flow-split convention, and inviting advice if they have any.

### D10 — HIH color/format decisions: hidden -9999 sentinel in Score layers, and FinalLocation moves from raster to vector
**Date**: 2026-09-03
**Status**: Decided (hfu confirmed the process/direction; the FinalLocation raster→vector call itself is this session's technical judgment, made explicitly to be checked, not assumed final).

**Context**: per hfu's explicit instruction, the HIH color/encoding question was investigated *before* running any conversion (unlike GAEZ, where D7/D8's color problems were found and fixed after a first bad build). Downloaded and inspected real DR Congo COGs directly (`COD-LOCATIONSCORE-MAIZE`, `COD-FINALLOCATION-MAIZE`, `COD-LOCATIONSCORE-CASSAVA`) rather than inferring from STAC metadata alone, since — unlike GAEZ's `AEZ33`, whose official colormap was fully embedded in `renders.data.colormap` — HIH's STAC `renders` property only carries a `colormap_name` reference with no embedded values, meaning the actual palette (if any) lives only inside FAO's own WMTS renderer, not in data this session can read directly.

**Finding 1 — Score layers (`LOCATIONSCORE`/`SCORE`, Float32) carry a hidden second nodata sentinel**: `COD-LOCATIONSCORE-MAIZE`'s declared `NoData` is the standard float32 sentinel (`-3.4028235e+38`), but a histogram revealed **8,204,781 pixels — nearly half of all "valid" pixels — actually hold the literal value `-9999`**, an undeclared secondary "excluded area" flag GDAL doesn't recognize as nodata. This single value was dragging the naive statistics wildly negative (`Mean=-4717`) and would have wrecked any naive min/max color stretch. Genuine score values cluster in roughly 0–76 (this commodity/country's actual range). The identical 8,204,781-pixel count reappears in `COD-FINALLOCATION-MAIZE`'s own histogram, confirming it's one shared "outside the studied area" mask reused across a country's whole HIH layer family, not a per-file quirk.
- **Decision**: treat `-9999` (and the declared float32-nodata) both as transparent/excluded when building the color table — not just the declared NoData value alone.
- **Decision**: use a **fixed universal domain, 0–100**, for every Score layer's color ramp, not a per-file auto-stretched min/max — so different commodities and countries stay visually comparable (a weak-suitability country shouldn't look "hot" just because its own file has a lower local maximum). A generic, colorblind-safe sequential ramp (not a diverging one — suitability is a one-directional concept) will be used, deliberately *not* claimed as FAO-official (unlike GAEZ's real colormap, none exists here) — this is this repo's own reasonable convention for continuous suitability data, consistent with common GIS practice for this data type. Mechanically, scores will be rounded/binned to integer steps and baked into a ~101-entry GDAL color table, reusing the exact same PNG8-indexed pipeline validated in D8 rather than introducing a second encoding path for continuous vs. categorical data.

**Finding 2 — FinalLocation layers (Int16) are extremely sparse binary site-selection flags, not a raster-shaped dataset**: `COD-FINALLOCATION-MAIZE` has a clean, single, standard `NoData=-32768`, but only **417 pixels out of ~18.1 million hold the "selected site" value (1)** — everything else is either the shared `-9999` exclusion mask or genuine nodata. This is a single-digit-percentage-of-a-percent sparse point pattern, not a raster surface.
- **Decision (this session's call, explicitly flagged for hfu to override)**: convert FinalLocation layers to **vector point features (GeoJSON)**, not raster/PMTiles, reversing D9's assumption that all 14 DR Congo commodity files (`-score` and `-final` pairs) would be built the same way. Reasoning: nearest-neighbor overview/pyramid downsampling (mandatory for any categorical/sparse raster per D8) can and likely would make a 417-pixel feature vanish entirely at most zoom levels a real map would actually be viewed at — the raster pipeline that works well for Score's continuous surface is the wrong tool for a handful of point-like selected sites. A point feature with attributes (commodity, country, score-at-site) is also a more directly useful UI primitive for "here is where FAO's analysis says to build this" than a nearly-invisible raster pixel cluster.
- This changes D9's file list in kind, not in name: the seven `hih-cod-{commodity}-final` names stay as planned, but as GeoJSON/vector sources rather than PMTiles raster sources. `hih-cod-{commodity}-score` stays raster PMTiles as originally planned.

### D9 — Final 23-file naming list for the first conversion batch (GAEZ + DR Congo-only HIH); other five HIH countries deliberately left undefined
**Date**: 2026-09-03
**Status**: Approved by hfu. Next: brief advance heads-up to `stars-fd` (not blocking), then start real conversion work on all 23.

**What**: hfu narrowed D8's six-country HIH scope down to **DR Congo only** for this first batch — "anything that doesn't involve DR Congo stays undefined for now," not decided against, just deferred. Re-deriving the naming list against DR Congo's actual 21 collections (one-to-one, no data loss) caught and fixed a real error in D8's proposal: `COD-ACCESS-URB` (plain travel-time accessibility) and `COD-ACCESS-URB-WGTH` (demand-weighted accessibility) are two distinct metrics that D8 had wrongly collapsed into one `hih-access-urban` name. Final 23-file list, flat Martin-style naming (D8), no country code on the fish-farming names since they're intentionally left as open mosaics other countries' data can be added into later without renaming:

- GAEZ-V5 (2): `gaez-aez33`, `gaez-aez57`
- HIH accessibility (3): `hih-access-urban`, `hih-access-urban-weighted`, `hih-access-port`
- HIH fish-farming (4, DR Congo-only data for now, name left country-agnostic): `hih-fishfarm-closed`, `hih-fishfarm-closed-final`, `hih-fishfarm-open`, `hih-fishfarm-extensive`
- HIH crop storage/livestock, DR Congo-specific, 7 commodities × {score, final} = 14: `hih-cod-{cassava,cocoa,coffee,maize,palmoil,wheat,livestock}-{score,final}`

Bhutan, Côte d'Ivoire, Central African Republic, Cameroon, and Republic of Congo's HIH data (D6's other five target countries) are explicitly **not named or scoped yet** — this list covers only what's being converted first.

**Process note**: per hfu, before starting actual conversion on these 23, send `stars-fd` a brief advance heads-up about the naming list (they may have naming/hosting advice worth hearing before files exist) — but don't block on a reply; proceed with conversion regardless. Also per hfu: the color-palette question for HIH's data (D8 solved this for GAEZ via its official STAC `renders.colormap`; HIH's own value structure — continuous suitability scores vs. GAEZ's discrete classes — hasn't been checked yet) should be settled *before* running any conversion, not fixed up afterward.

### D8 — Color encoding (PNG8 indexed, approved), flat Martin-style file naming (corrected from D7), and HIH's per-theme merge strategy
**Date**: 2026-09-03
**Status**: Color/encoding choice approved by hfu. Naming convention set. HIH merge strategy proposed from real data, pending explicit confirmation.

**Color encoding — approved**: D7's first colored build used `gdaldem color-relief` to bake FAO's official `AEZ33` colormap (found in the STAC item's `renders.data.colormap` property — the actual authoritative source, not `classification:classes`, which is empty for GAEZ-V5 unlike ESA WorldCover) into a real 4-band RGBA GeoTIFF. That produced a working but bloated 184MB PMTiles (vs. the uncolored 34MB baseline) — 4 bytes/pixel instead of 1, and full RGBA PNG compresses far worse than an indexed image for a ~33-color categorical map. Fix: attach the same official colormap as a genuine GDAL **color table** (PCT) to the original single-band class-code raster instead — via a hand-edited VRT (`<ColorTable>` with per-value `<Entry c1..c4>` entries, transparent for nodata=255) — so `gdal_translate -of MBTILES` emits real indexed-color PNG (PNG color type 3), keeping pixels at 1 byte each.
- First attempt at this still came out oversized (70.8MB) — root cause found: `-co RESAMPLING=NEAREST` had been added to `gdaladdo`'s overview-pyramid step but was **missing from the base `gdal_translate -of MBTILES` reprojection step itself**, letting GDAL's default resampling interpolate index values across class boundaries during the EPSG:4326→3857 warp — inventing meaningless intermediate class codes at every edge, which also happens to be exactly the kind of corruption categorical data must never undergo. Real lesson: **nearest-neighbor must be forced at every resampling step in the pipeline, not just the overview-building step** — the base warp has its own independent default.
- With that fixed: PNG8/indexed came out **36.5MB**, matching the uncolored baseline closely. A same-fix comparison against `TILE_FORMAT=WEBP, QUALITY=100` produced **92.2MB** — worse, and GDAL's MBTiles/WEBP writer has no true lossless option (`QUALITY` is a lossy-only knob), which is disqualifying on its own for categorical data regardless of size (hfu, emphatic: lossy compression on class-coded data is 厳禁 — strictly forbidden, corrupts values at boundaries). **Decision, approved by hfu: PNG8/indexed color**, not WebP, for all categorical FERSPAS layers (GAEZ and HIH alike) — smaller, and correct by construction rather than by trusting an encoder setting.

**File naming — flat, not nested (corrects D7)**: this session's first proposal (`ferspas57/aez33-1km.pmtiles`, one subdirectory per project) was rejected — Martin's own conventions don't practically support directory nesting for registered tile sources, and hfu characterized Martin's culture as closer to Unix/DOS: short, flat, unceremonious names. Landed on: **short, flat, collection-ID-derived names with no path nesting** — e.g. `aez33`, `aez57` for the GAEZ layers (dropping the `ferspas57-`/`gaez-v5-` prefixes entirely, since flat short names are the house style, not because provenance doesn't matter — provenance lives in TileJSON `attribution`/metadata instead, per `cafebabe`'s D6 finding that this costs nothing extra in MapLibre).

**HIH's cross-country merge strategy**: hfu's stated ideal is that "the same kind of layer" should be one PMTiles regardless of country — Congo or Bhutan, if it's conceptually the same thing, it's the same layer. Checked this against the actual naming/content of all HIH collections across the six D6 target countries (DR Congo, Bhutan, Côte d'Ivoire, Central African Republic, Cameroon, Republic of Congo) rather than assuming it holds:
- **Fish-farming siting (closed/open/extensive, +photovoltaic variant) merges cleanly** across 5 of 6 countries (all but land-locked, non-fish-farming Bhutan) — the methodology and sub-types are genuinely the same everywhere, modulo cosmetic naming-string differences (`FISH-` vs `FISHFARM-`, `EXTNSV` vs `EXTENSV`). Proposed flat names: `hih-fishfarm-closed`, `hih-fishfarm-open`, `hih-fishfarm-extensive`, `hih-fishfarm-closedpv`.
- **Weighted urban accessibility merges across 4 of 6** (DR Congo, Côte d'Ivoire, Central African Republic, Republic of Congo) once a real naming inconsistency is normalized (`WGTH` vs `WGHT` — a plain typo divergence between countries, not a semantic difference). Proposed: `hih-access-urban`. Port accessibility (DR Congo + Central African Republic only) and city accessibility (Bhutan only) are separate, much smaller-footprint layers.
- **Crop-storage siting mostly does not merge** — the six countries studied genuinely different commodities (DR Congo: cassava/cocoa/coffee/maize/palm-oil/wheat; Bhutan: rice/citrus/asparagus/cereal/tropical-fruit/vegetables; Côte d'Ivoire: broad cereal/fruit/vegetable buckets; Central African Republic: cassava only, abbreviated `CSSV`; Cameroon and Republic of Congo have none at all in this theme). There is essentially no cross-country overlap in which commodity was studied, so forcing a merge would misrepresent the data rather than simplify it. Proposed: keep these per-country (e.g. `hih-cod-maize`, `hih-btn-rice`), not merged, at least until/unless FAO's own HIH coverage grows enough commodity overlap between countries to justify it. Dairy-processing and slaughterhouse siting are similarly thin (2–3 countries each, with their own naming inconsistencies — e.g. Bhutan's own `LOCATIONSCORE-DIARY`, a typo for `DAIRY`, inside its own data) and are being left per-country for the same reason.

**Not yet confirmed**: the HIH merge/naming proposal above reflects this session's analysis of real data, but — unlike the PNG8 color-encoding decision, which hfu explicitly approved — hasn't yet received the same explicit sign-off from hfu as a final naming scheme.

### D7 — Tentative: host the GAEZ PMTiles on `stars.optgeo.org` (Martin-served), not client-side `pmtiles.js`
**Date**: 2026-09-03
**Status**: Tentative (hfu's own word: 仮決定) — a working direction, not a closing of D2's broader tile-serving-architecture question, and nothing has actually been deployed to `stars` yet.

**What**: First real end-to-end technical validation of D3/D6's option (C), run locally against the actual GAEZ-V5 `AEZ33-1KM` COG (33.2MB):
1. `gdal_translate -of MBTILES` reprojects to Web Mercator and tiles it — but a first attempt without explicit overview building produced a single-zoom (z7-only) archive; GDAL's MBTiles driver needs `gdaladdo` run afterward (tested `-r nearest` at factors 2/4/8/16/32/64/128) to actually populate the full z0–z7 pyramid. This is a real, previously-undocumented-here gotcha, not just a config nicety.
2. `pmtiles convert` (the same Go CLI `mapterhorn-japan-bridge` recommended, D2) turns that MBTiles into a PMTiles archive: **34,254,666 bytes (34.25MB)** for `AEZ33-1KM`, `pmtiles verify` clean, z0–z7, 21,845 addressed tiles. `AEZ57`'s source COG is a near-identical 32.6MB, so expect a similarly-sized archive — not yet converted as of this writing.
3. Served locally via `pmtiles serve` (the CLI's own Z/X/Y HTTP proxy, not the `pmtiles.js` browser library) and loaded into MapLibre GL JS as a plain `raster` source — worked cleanly, `200 OK` on every tile request.
4. A first attempt to instead load the raw `.pmtiles` file directly in-browser via the `pmtiles.js` protocol, served by a plain Python `http.server`, **failed** ("Server returned no content-length header or content-length exceeding request") — Python's stock `http.server` doesn't implement HTTP Range requests properly, which `pmtiles.js`'s client-side range-reading depends on.
5. Direct pixel inspection of a served tile confirmed the underlying data is intact and correctly varied (e.g. `(26,26,26)` RGB = class 26, `(255,255,255)` = the declared nodata value) — the map initially rendering as near-solid black was expected and correct: raw class codes 1–33 as literal grayscale intensity are almost indistinguishable from black to the eye, not a data or pipeline fault. Real-looking cartography still needs the actual AEZ33/57 class legend (not present in FERSPAS's own STAC metadata — sourcing it is a separate open task) applied via a client-side `raster-color` expression, plus nodata made transparent instead of white.

**Decision**: given (a) the archive size (~34MB, comfortably inside "tens of MB," hfu's own threshold for this call) and (b) finding (4) above — a live, first-hand demonstration that client-side `pmtiles.js` is the more fragile path (Range-request-dependent, breaks against naive servers) versus a real server (Martin on `stars.optgeo.org`) doing the same PMTiles-reading job server-side and handing the client a plain tile/TileJSON endpoint — hfu's tentative call is to **host this PMTiles archive on `stars.optgeo.org` via Martin, not as a client-fetched static `.pmtiles` file with `pmtiles.js`**. hfu's stated reasoning: attaching `pmtiles.js` to the site "isn't elegant" (エレガントではない) — this session's own accidental reproduction of `pmtiles.js`'s server-requirements fragility is a concrete technical point in favor of that instinct, not just an aesthetic preference.

**Still not decided**: this is scoped to the small, self-generated GAEZ PMTiles specifically — it does not resolve D2's broader question (which was largely about much larger/raw-COG scenarios) or commit HIH's data to the same path yet. Nothing has actually been uploaded to `stars.optgeo.org` — `stars-fd` asked in D2 that any concrete step be run past hfu first, and separately, actually placing something on shared infrastructure is itself a to-be-had conversation, not a unilateral action, per D6's own reasoning for staying local-only until it is.

### D6 — Shortlist finalized (DR Congo-led HIH across 6 countries + GAEZ AEZ33-1KM/AEZ57); moving into local technical prototyping
**Date**: 2026-09-03
**Status**: Content scope fixed. Build phase starting, against a local server only — hosting platform and client-site deployment location remain deliberately undecided (see below).

**What**: Based on D5's audit, hfu fixed the content scope:
- `HWSDV201` dropped from the GAEZ-V5 shortlist (no license link — D5). Remaining shortlist: **`AEZ33-1KM`, `AEZ57`**.
- HIH scope is **all six countries with complete data** from D5 — DR Congo (21), Bhutan (17), Côte d'Ivoire (15), Central African Republic (11), Cameroon (7), Republic of Congo (5) — not narrowed to DR Congo alone. hfu has direct personal familiarity with this region (recent travel to DR Congo specifically), which is part of why this set feels tractable rather than arbitrary. Within the six, **Bhutan is deprioritized** for being geographically disconnected from the other five (all Central/West African) — a coherent regional narrative is easier to build across neighboring countries than across one isolated one, even though Bhutan's data is equally complete.
- **DR Congo + GAEZ (`AEZ33-1KM`/`AEZ57`) is the starting technical target** — first real COG/PMTiles prototyping work begins here.

**Build convention**: hfu confirmed dwg7 has an established convention for shipping a MapLibre-based static site — normally built into `docs/` for direct GitHub Pages hosting, matching `layers-martin`'s pattern. `dwg7/cafebabe` (the cross-project pattern-pool session) was asked (2026-09-03) for the specifics before this session starts writing client code. Its reply, in brief (full detail lives in `cafebabe`'s own `patterns/` and `STACCATO-CONTEXT.md` — not duplicated here per this repo's own knowledge-flow-split convention):
- No strict single standard, but a converged majority pattern: bundler-free static `docs/` (no build step) is what most dwg7 projects use for GitHub Pages, lowest friction.
- MapLibre GL JS version depends on bundling: no-bundler/plain `<script>` setups should stay on v5.x or v4 (v6 dropped UMD distribution, ES-modules only — `unpkg.com/maplibre-gl@6.x` 404s for a plain script tag); bundled (Vite/webpack) setups should use v6+.
- **"Runtime hydration" style pattern** (used by `sas0`, `kitavolca`): don't copy a static `style.json` locally — fetch the upstream style JSON (in our case, whichever of `stars.optgeo.org`/Source Cooperative gets chosen) at page load and push/splice in project-specific sources and layers live, with `Promise.allSettled` + `AbortController` fallbacks. Avoids drift from re-copying on every upstream change.
- This whole exercise is a concrete instance of `staccato-spec`'s 4-party model (User/Staff/Cartographer/Library) — `cafebabe` flagged one relevant structural finding: putting `stars.optgeo.org` and `layers-martin` in the same Map Intent needed **no catalog-aggregation component** at all, since `catalog_context.active_catalogs` is already an array — i.e. the architecture already supports multiple parallel Library sources, which softens the stars-vs-Source-Cooperative choice in D2 (both could coexist later rather than being strictly exclusive).
- Practical notes: TileJSON's own `attribution` field is picked up by MapLibre automatically (zero extra implementation cost) — worth using given this repo's attribution commitments; `hash: "map"` namespaces the URL hash to avoid colliding with app state; `stars.optgeo.org`'s CORS implementation *echoes* the request's `Origin` header rather than a static wildcard, so verifying it needs `curl -H "Origin: ..."` explicitly, not a bare `curl -I`.

**Deliberately deferred despite starting to build**: this session is proceeding with local-server-only testing for now, *not* deploying into `docs/` or picking between `stars.optgeo.org` and Source Cooperative yet, even though `docs/` is the known eventual destination shape. Reasoning ties back to D2's earlier point: committing infrastructure (a hosting platform, a public URL) is a communication act with real people (`stars-fd`, and potentially FAO CSI), not just a technical choice, and hfu wants that conversation had properly before anything is actually deployed anywhere. Local testing lets real technical progress happen without pre-empting that.

**Role mapping onto `staccato-spec`'s 4-party model, hfu, same day**: the site being built is a **Cartographer**; the AI-generated GAEZ×HIH narrative (D4) is delivered by a **Staff**. The framing hfu added: this is a Staff that *narrates* rather than one that *listens* — not a reactive Q&A agent waiting on a User's question (which is closer to how FAO's own AIDA reads, per D4's finding), but one that proactively tells a story and guides the User through it. Both are still intermediary/broker roles between User and Library data in Staccato's sense ("取次役" either way) — the novelty hfu is pointing at is the direction of initiative, not the role itself. This feels like a genuine architectural distinction worth eventually raising with `staccato-spec` (a "Staff" sub-pattern, not yet documented there as far as this session knows) rather than staying siloed here — tracked as owed knowledge-flow in `HANDOVER.md`, not yet acted on.

---

### D5 — Source-data audit for the D3/D4 shortlist: licensing is clean, but 30% of HIH's collections are empty stubs
**Date**: 2026-09-03
**Status**: Audit complete for the shortlisted candidates. Confirms GAEZ-V5's shortlisted layers and HIH are viable, but corrects D4's country-coverage read and narrows the realistic HIH demo scope to a handful of countries.

**What**: Per-collection license + item-count + total-size audit (`/collections/{id}` for license, `/collections/{id}/items?limit=50` for item count and summed `file:size`), run against GAEZ-V5's five shortlisted layers and all 131 HIH collections.

**GAEZ-V5 shortlist** (5 layers, 1 item each):
| Layer | Size | License |
|---|---|---|
| `AEZ33-1KM` | 33.2 MB | CC-BY-4.0 |
| `AEZ57` | 32.6 MB | CC-BY-4.0 |
| `HWSDV201` | 28.0 MB | **no license link** |
| `SLOPE-MED` | 80.4 MB | CC-BY-4.0 |
| `LR-IRR` | 259.8 MB | CC-BY-4.0 |

`HWSDV201` (Harmonized World Soil Database) has no `rel:license` link at all — per this repo's existing policy (`HANDOVER.md`: no license link means unresolved, not implicitly open), it should be excluded or flagged, not assumed usable, unless resolved directly with FAO. The two lead candidates from D3 (`AEZ33-1KM`, `AEZ57`) are both cleanly CC-BY-4.0 and small (~33MB each).

**HIH, all 131 collections**: licensing is completely clean — **131/131 are CC-BY-4.0**, no exceptions, no missing links. Total data volume across everything that actually exists is only **~1.46GB** (dominated by one outlier, see below) — comfortably small for this project's purposes.

**Correcting D4**: item-level audit overturns the country-coverage picture D4 stated from collection *counts* alone. **39 of 131 HIH collections (30%) return zero STAC items** — verified directly (`"features": []`) on samples, not a script artifact. Critically, this splits **countries, not collections, cleanly into all-or-nothing**:

| Country | Collections | With actual data |
|---|---|---|
| DR Congo (COD) | 21 | **21 (complete)** |
| Bhutan (BTN) | 17 | **17 (complete)** |
| Côte d'Ivoire (CIV) | 15 | **15 (complete)** |
| Central African Republic (CAF) | 11 | **11 (complete)** |
| Cameroon (CMR) | 7 | **7 (complete)** |
| Republic of Congo (COG) | 5 | **5 (complete)** |
| Djibouti (DJI) | 13 | 8 (partial) |
| Benin/Cabo Verde/Belize/Colombia/Cuba | 8 total | all present |
| **Ghana, Eritrea, Guinea-Bissau, Gabon, Iraq** | 34 total | **0 — entirely empty** |

D4's claim that Ghana and Eritrea had "the full multi-theme toolkit" (11 collections each) was **wrong** — both are 100% empty stubs. This is corrected here rather than silently edited into D4.

**DR Congo is the strongest candidate for the flagship GAEZ×HIH narrative** (D4): all 21 collections have real data, covering crop storage/processing siting for cassava/maize/cocoa/coffee/palm-oil/wheat, livestock siting, fish-farming siting, and accessibility — the full thematic breadth in one country. Total size: 491.8MB, all CC-BY-4.0. Internal size pattern confirms the Score/Final-Location structure from D4: "LOCATIONSCORE"/"ACCESS"/"FISH-SCORE" layers run 22–50MB (continuous suitability rasters), "FINALLOCATION"/"FISH-FIN" layers run 0.33–0.37MB (sparse, selected-site rasters).

One size outlier investigated and explained, not a data problem: `COL-ACCESS-CITY` (Colombia) is 809.87MB, ~16× every other HIH layer — checked directly, it's a UTM-projected, ~90m-resolution, full-national-extent single raster (20632×15077px), i.e. explained by finer resolution over a large country, not corruption or a fluke.

**Implication for D4's plan**: the GAEZ×HIH cross-narrative should be prototyped around **DR Congo** specifically (complete, thematically rich, well within a workable size) rather than attempting to generalize across all 17 nominal HIH countries immediately — a third of that nominal breadth doesn't actually exist yet in FERSPAS.

---

### D4 — Value proposition reframed as an open, freely-reimplementable analog to FAO's own AIDA, scoped to a GAEZ×HIH cross-narrative
**Date**: 2026-09-03
**Status**: Direction set; the underlying technical/content sizing this depends on is being gathered next (see "Still needed").

**What HIH actually contains, in full** (131 collections surveyed by ID + title — **note**: collection *counts* below were later found to overstate actual usable content; see D5's item-level audit, which found 39 of these 131 to be empty stubs with zero STAC items): coverage spans 17 countries, extremely unevenly by collection count — DR Congo (21 collections), Bhutan (17), Côte d'Ivoire (15), Djibouti (13), Central African Republic/Eritrea/Ghana (11 each) *appeared* to have FAO's full multi-theme Hand-in-Hand siting toolkit by collection count alone; Benin, Cabo Verde, Belize, Colombia, Cuba, Iraq have only one or a few one-off layers. Five recurring themes: crop storage/processing siting (52 collections, commodity-specific — cereal, cassava, cocoa, coffee, palm oil, maize, wheat, beans, fruit, vegetables), fish-farming siting (32, split by system type — closed/open/non-intensive — and species — African Catfish, Nile Tilapia — with a photovoltaic-power variant), accessibility (19, travel-time-cost to cities/ports plus demand-weighted composites), dairy-processing siting (14), slaughterhouse siting (14). Almost every theme comes as a **Score** (continuous suitability raster) paired with a **Final Location** (the derived/selected actual site) — i.e. HIH is not a pile of miscellaneous maps but a consistent, repeated siting-decision methodology applied per country per commodity. All raster (not vector, as one might expect for "site" data), and — unlike GAEZ — clipped to national/sub-national extent, so individual files are tiny (measured: 0.37MB–7.3MB per collection sampled).

**Two observations feeding the reframe**:
- hfu, from `dwg7/chukei` experience: generative AI is already known to be good at exactly this task — inferring thematic classification from cryptic layer-name codes (chukei's own domain-vocabulary work is the precedent). HIH's `{COUNTRY}-{STAGE}-{COMMODITY}` naming convention (e.g. `COD-FINALLOCATION-MAIZE`) is squarely this kind of problem, which is why an AI-narrative layer over it is plausible, not speculative.
- Checked directly (WebSearch, 2026-09-03) whether FAO has already built the "AI narrates a path through buried geospatial data" idea this session arrived at for HIH: **yes, largely.** FAO's own **AIDA** (Agro-Informatics Digital Assistant, `aida.fao.org`) explicitly frames its mission as changing the starting point "from 'Where is the data?' to 'What do I need to know?'" — the same value proposition, in FAO's own words, connecting statistics/water/forests/animal-health/prices/**geospatial products**/advisory knowledge through one natural-language entry point that can return interactive maps. Depth of actual geospatial-layer narrative guidance (e.g. whether it does anything like walking a user through HIH's fragmented per-country siting toolkit specifically) isn't documented publicly. Per DWG5's `yuiseki` in `UNopenGIS/7#932` (June 2026), AIDA is **closed-source and closed-system** with almost no public technical documentation.

**Decision**: given AIDA already exists and stakes out very similar territory, ferspas57's honest differentiator is not "an idea FAO hasn't had" — it's building an **open, freely-reimplementable analog**, in the spirit of the kind of open-alternative-to-closed-system thinking hfu associated with something `yuiseki` has discussed on a radio/podcast appearance (referenced only in spirit here, not by specific content, since this session hasn't verified that source directly). Concretely, scoped narrowly rather than attempting to rival AIDA's full breadth:
- **GAEZ interpretation assistance**: AEZ33/AEZ57's classification codes are opaque without domain expertise — a narrative layer that turns "you clicked Zone 14" into a plain-language explanation of what that zone means agronomically is a real, self-contained piece of value, independent of HIH.
- **A GAEZ×HIH cross-collection narrative**: GAEZ answers "why is this place suited to X"; HIH answers "where specifically to build Y here." Weaving the two into one place-based story (e.g. "this part of DR Congo's agro-ecological zone suits cassava; here's where FAO's Hand-in-Hand analysis found the best cassava-storage site") is a cross-collection synthesis that showcases Staccato's actual architectural thesis — unifying heterogeneous catalog content into one experience — rather than a single dataset's UX, and isn't something a single-dataset viewer (or, as far as documented, AIDA) is confirmed to already do.
- This AIDA finding is DWG5-relevant and should be routed back to `UNopenGIS/7#932` per `CLAUDE.md`'s knowledge-flow-split guidance, independent of what ferspas57 does with it.

**Still needed before any technical build**: a detailed accounting — count, total size, and per-collection license — of the actual source data this hypothesis depends on: GAEZ-V5's shortlisted layers (D3: `AEZ33-1KM`, `AEZ57`, plus `HWSDV201`/`SLOPE-MED`/`LR-IRR` if still in play) and **all 131 HIH collections** (not just the samples characterized above). This is the next concrete task, requested by hfu explicitly before any conversion prototyping begins.

### D3 — "Good part" content shortlist: GAEZ-V5's global layers + HIH; WAPOR-3 and ESA WorldCover set aside for now
**Date**: 2026-09-03
**Status**: Shortlist fixed; final pick(s) to convert first still to be narrowed from it.

**What**: After surveying and sizing several candidate families from FERSPAS's 1282-collection catalog (see `HANDOVER.md` and D2), hfu set the working shortlist for what ferspas57 should actually convert and demonstrate:

- **GAEZ-V5**'s global ~1km (30 arc-second) thematic layers — `AEZ33-1KM` and `AEZ57` (Agro-Ecological Zones classification, 33/57 classes), `HWSDV201` (Harmonized World Soil Database), `SLOPE-MED`, `LR-IRR` (irrigable land resources), `ELEV-ALOS-30M`, and the rest of the ~108-collection family. Measured sizes range 28MB (`HWSDV201`) to 521MB (`ELEV-ALOS-30M`), with the flagship classification layers (`AEZ33-1KM`, `AEZ57`) at ~33MB each — a genuinely workable range for a real COG/PMTiles technical exercise, unlike ESA WorldCover's 182GB.
- **HIH** (the Hand-in-Hand initiative) — despite, and in part *because of*, its ~131 collections being fragmented into narrow, country×sector-specific site-suitability scoring layers (e.g. closed-pond fish-farming viability scores per African country: `CAF-FISHFARM-SCORE-CLOSEDPV`, `CMR-FISHFARM-SCORE-CLOSEDPV`, etc.) — see the reasoning below for why fragmentation argues *for* inclusion here, not against it.

Set aside for now (not eliminated, just off the active shortlist):
- **WAPOR-3** — a genuine FAO flagship (WaPOR water-productivity program) and reasonably sized at its L1/L2 continental tier, but judged "too specialized" (専門的すぎる) a subject to be a good first demonstrative target for a general audience.
- **ESA WorldCover** — this session's earlier top pick, now understood as the wrong choice: it is commodity data, already broadly available via AWS Open Data, Copernicus, and Element84 Earth Search independent of FERSPAS entirely. Converting it via FERSPAS wouldn't showcase anything distinctive to FAO or this collaboration.

**Why**: Three criteria converged, refined over the course of this investigation (hfu, verbatim in spirit): *"コンテンツとして魅力的で、データが大きすぎず小さすぎず、技術実証にちょうど良いのがgood partの定義"* (content that's genuinely appealing, data that's neither too big nor too small, technically well-suited to a proof of concept):

1. **Non-commodity, flagship content** — rules out ESA WorldCover; GAEZ (FAO+IIASA, running since the 1980s) and HIH (FAO's cross-cutting 2019 flagship initiative, its own dedicated geospatial platform) both clear this bar.
2. **Size in the workable middle** — ruled out both extremes found during this investigation: GAEZ's own `AEZ33` at native 0.5° resolution (733KB, too coarse/trivial to be a meaningful technical test) and ESA WorldCover (182GB, requiring special-case zoom-capping and infrastructure planning just to attempt). GAEZ's 1km family sits comfortably in between.
3. **A reframing of "handling ease" itself, prompted by hfu**: HIH's fragmentation was initially read (by this session) as a liability weighing against including it — 131 narrow layers are harder to present as one coherent thing than GAEZ's ~108 clean global variables. hfu inverted this reasoning: Staccato's whole premise, per `staccato-spec`, is to move *away* from being another catalog site or geoportal (脱カタログサイト、脱ジオポータル) — and a geoportal's defining failure mode is exactly that fragmented, narrow data gets buried in endless drill-down and never surfaces. HIH's fragmentation is therefore not a reason to exclude it; it is the ideal test case for whether ferspas57's actual differentiator — an AI-generated narrative that visually guides a user to the one specific buried layer they need, rather than a static catalog listing — works in practice. Converting only the easy, already-coherent case (GAEZ) would prove the tile-serving pipeline but not Staccato's actual value proposition. Converting only HIH, conversely, would risk having no clean technical baseline to build the harder UX against. Hence both are on the shortlist together, not as alternatives.

**Technical note**: for GAEZ-V5's global grid family, `mapterhorn-japan-bridge`'s native-leaf-tiles-then-separate-downsampled-pyramid technique (from its Japan DEM PMTiles work — see D2) was flagged by hfu as directly transferable, if GAEZ's various layers need to be presented as one seamless, consistently-tiled family rather than ~108 independently-built archives.

**Within-GAEZ-V5 narrowing (hfu, same day)**: of the candidate layers sized above, the Agro-Ecological Zones classification itself (`AEZ33-1KM` / `AEZ57`) is judged the "catchiest" (キャッチー) — more immediately legible and compelling than terrain or soil layers from the same family. `ELEV-ALOS-30M` (elevation) is specifically deprioritized for a different reason than the others: it would directly overlap (モロ被り) with `mapterhorn-japan-bridge`'s own existing scope (DEM/terrain PMTiles), so pursuing it here would duplicate a sibling dwg7 project's work rather than add something distinct. `HWSDV201` (soil) and `SLOPE-MED` remain on the table but are, so far, less compelling as the lead choice than the AEZ classification layers.

**Still open**: the final narrowing from {GAEZ-V5's AEZ33-1KM/AEZ57 (leading), HWSDV201, SLOPE-MED, LR-IRR; HIH} to whichever collection(s) actually get converted and demoed first; the HIH "AI narrative + visual guidance" UX is a stated direction here, not yet designed in any concrete form; D2's tile-serving architecture question (COG-direct vs. mirrored-COG vs. self-hosted PMTiles vs. FAO's WMTS wrapper) remains independently unresolved and will need an answer for whichever content ends up chosen here.

### D2 — Tile-serving architecture for FERSPAS raster collections: options surveyed, not yet decided
**Date**: 2026-09-03
**Status**: Open. Investigation and option-framing only — no option has been chosen.

**What we found by surveying the live catalog** (paged all 1282 collections via `https://data.review.fao.org/geospatial/search/stac/collections`):

- No orthophoto/aerial-imagery collection exists anywhere in the current 1282. The Niedersachsen DOP dataset hfu noted in `UNopenGIS/7#932`'s June 2026 comment (external data, CC-BY-4.0, provided by Germany's Niedersachsen state survey office LGLN, not FAO) was present in FERSPAS's catalog then but is **confirmed absent now** — removed at some point between June and September 2026, not merely renamed or missed by keyword search.
- Across all 33 top-level `fao-gismgr:*` sub-catalogs (sampled one representative item per prefix), asset structure is strikingly uniform: every item has a `data` asset (Cloud-Optimized GeoTIFF) plus `preview`/`thumbnail` assets served through a single FAO-run WMTS endpoint, `https://data.apps.fao.org/map/wmts/wmts`. No vector assets were found anywhere in the sample. This means FERSPAS's practical heterogeneity (as STAC-the-spec allows) is much lower than the STAC-in-general case — the catalog appears to be generated by one internal FAO GISMGR pipeline that normalizes everything to this same shape.
- That WMTS endpoint (self-identified in its `GetCapabilities` response as **"FAO WMTS WRAPPER"**, provider **FAO CSI**, contact **Mohamed Megahed** — the same team behind the June 2026 DWG5 presentation) serves every sampled layer through the standard `GoogleMapsCompatible` (Web Mercator, 256px, z0–z24) tile grid — i.e. it is already a de facto XYZ tile server for the whole catalog, confirmed working (`200 image/png`) against layers from `ESA`, `CRTB`, `GAEZ-V5`, `GLOSIS`, `HIH`, and `WAPOR-3`.
- Measured performance of that WMTS wrapper (ESA WorldCover 2020, `fao-gismgr:ESA:raster:maps:WC2020`): 200ms–4s per tile depending on zoom, one transient 503 observed near native-resolution zoom (not reproducible on retry), and — critically — `cf-cache-status: DYNAMIC` on every request including immediate repeats, meaning Cloudflare is not edge-caching these tiles at all despite a `Cache-Control: public, max-age=3600` header. Every tile request reaches FAO's live App Engine backend.
- The underlying COG itself (`gs://fao-gismgr-esa-data/DATA/ESA/MAP/ESA.WC2020.tif`, 195,668,306,548 bytes ≈ 182GB, global, ~9.28m/px native resolution) is a genuinely well-formed Cloud-Optimized GeoTIFF: 256×256 internal tiling, LZW compression, and a full 15-level internal overview pyramid (down to 132×66px). `storage.googleapis.com` correctly serves HTTP range requests (`206 Partial Content`, `Accept-Ranges: bytes`). **However, the bucket has no CORS configuration** (`access-control-allow-origin` absent from every response tested) — so a browser-side MapLibre COG reader cannot range-read it directly today, even though the file itself is structurally ready for that access pattern.

**Options on the table** (none selected yet):

- **(A) Direct client-side COG range access against FAO's own GCS bucket.** Best long-term outcome — no duplication, no extra infrastructure, matches Staccato's "reference, don't copy" philosophy most purely. Blocked today solely by the missing CORS header, which is FAO's to fix, not ours.
- **(B) Mirror the raw COG (unconverted) onto our own storage with CORS enabled**, e.g. GitHub or Source Cooperative — a temporary stand-in we'd disclose to FAO and retire once they enable CORS themselves. Not pursued further (option C was chosen instead — see D7 onward), so not elaborated here beyond the two concerns that mattered at the time: the mirror's own CDN might not even outperform GCS+Cloudflare, and mirroring FAO's data onto third-party infrastructure without their buy-in needs to be proposed transparently, not done silently, per this repo's "Positive intent" stance.
- **(C) Convert to our own zoom-capped PMTiles and host statically**, mirroring `layers-martin`'s static-hosting pattern but adding an actual conversion step (since, unlike GSI's source, FERSPAS's source is COG, not already-tiled). Full performance and hosting control; smallest diplomatic footprint since it's a clearly-derived, attributed display product rather than a raw-data copy; but full-native-resolution global PMTiles is infeasible for something the size of ESA WorldCover (182GB) — would need a deliberately capped max zoom (and possibly per-collection sizing work) to be practical.
- **(D) Consume FAO's existing WMTS wrapper directly, live.** Zero engineering cost, works today, but the measured latency (up to several seconds/tile) and complete absence of edge caching make it unsuitable as a production path — and routing real end-user traffic through it risks being a real burden on FAO CSI's own infrastructure, which cuts against this repo's reciprocity framing. Reasonable only as a low-volume prototyping/demo path, not a shipped architecture.

**Why this is being recorded now, unresolved**: hfu's framing is that this design reasoning — done logically, fairly, and tastefully — may itself be a piece of value this repo delivers, independent of which option is ultimately chosen; possibly even shareable back to FAO CSI as constructive feedback (the CORS gap in particular is something fixing would benefit the whole ecosystem, not just this repo). Recording the option space and evidence now, before forcing a premature decision, keeps that reasoning legible for whoever continues it.

**Two clarifications from hfu on 2026-09-03, refining but not resolving the above**:
- On option (B)'s "duplication" question: whether a mirror is *legally* permitted by FERSPAS's license is a separate axis from whether making one *without telling FAO* is the right thing to do. hfu's framing: doing it silently would be legally fine but relationally 水臭い (distant/standoffish — the kind of move that quietly erodes trust between collaborators) given this repo's stated reciprocity stance. If a mirror is ever built, it should be proposed to FAO transparently, not simply shipped.
- (B) and (C) are confirmed as the two main contenders. But hfu explicitly named keeping (A) and (D) "in view" rather than discarding them — i.e. staying ready to adopt (A) the moment FAO fixes CORS, and treating (D) as a legitimate (if limited) fallback rather than a non-option — as itself characteristic of dwg7's way of working: holding the full option space in mind rather than collapsing to a single path prematurely.

hfu also decided (B) and (C) both warrant actual hands-on prototyping, as tests of two different technologies rather than a pick-one call: (B) tests the current state of client-side COG-in-MapLibre reading technology; (C) tests the practical feasibility of dwg7's static/distributed-hosting vision applied to a genuine COG-sourced (not already-tiled) dataset. Two peer sessions were consulted for this:

**`stars-fd` on `stars.optgeo.org`** (asked 2026-09-03 what the platform is and whether it fits option B): it is hfu's personal Raspberry Pi (aarch64, Debian 13) running Martin, config-of-record in `hfu/stars` on GitHub with `stars-fd` as PR gatekeeper, exposed via a Cloudflare named tunnel as `stars.optgeo.org` (tiles/styles, Martin) and `depot.optgeo.org` (raw file browsing, Caddy, already `Access-Control-Allow-Origin: *`). Findings directly relevant to D2:
  - Martin's COG support is an opt-in `unstable-cog` Cargo feature **not present in the current production build (v1.14.0)** — so Martin-as-COG-tile-source is not available today regardless of hosting choice.
  - **Critical, previously-unknown risk for option (B)**: Cloudflare's proxy in front of `depot.optgeo.org` silently drops `Range` requests above some threshold between 190GB and 424GB, returning `200` (whole file) instead of `206`. Verified directly against origin (fine at 424GB) vs. through the public Cloudflare-fronted URL (76KB→206 OK, 190GB→206 OK, 424GB→200/Range ignored). A browser attempting to range-read a COG above that threshold through this specific host would try to download the whole file instead — unusable for datasets in that size class (ESA WorldCover's 182GB is close to, but apparently under, the danger zone; would need re-verification specifically, not assumed safe by proximity alone).
  - Option (C) is *not* subject to this problem: Martin does the range-reading server-side and returns normal tile responses to the client, regardless of source file size. Already proven at this kind of scale — `stars.optgeo.org` currently serves 168–198GB of Overture Maps data this way (as a remote PMTiles source, no local copy).
  - Capacity: 397GB free of 1.8TB on the host filesystem as of this check (fluctuates).
  - `stars-fd`'s own assessment: for a large (100GB+) permanent COG mirror, a purpose-built platform (e.g. Source Cooperative, S3-based, designed around CORS+Range) is more straightforward than a single Raspberry Pi with the Cloudflare quirk above; for small-to-medium (tens of MB–a few GB) PMTiles, `stars.optgeo.org` is already a proven, comfortable fit (cites the Vientiane zoning-data project). It explicitly asked that any concrete next step be run past hfu first, since the host is shared infrastructure across several dwg7 projects.

**`mapterhorn-japan-bridge/japan-geotiff-dem`** (asked 2026-09-03 for its GeoTIFF→PMTiles conversion know-how, for option C — it has built a ~314GB clustered PMTiles archive from national-scale GSI elevation DEMs at 1m/5m/10m). Its data is continuous (elevation), ours would be discrete (classification), so not everything transfers, but the operational lessons are directly usable:
  - Tooling: GDAL/rasterio (Python) for reprojection/aggregation, Go `pmtiles` CLI (`cluster`/`merge`/`verify`/`show`) for archive assembly, Python `pmtiles` library for bulk merges. No single-pass `gdal2tiles`-style descent.
  - Architecture lesson (conditional): if source resolution/coverage varies across the collection, build native-resolution leaf tiles first, then a *separate* downsampled-pyramid pass reading from those leaves — not relevant if a given FERSPAS collection is uniform-resolution throughout, but worth checking per collection before assuming a simple top-down build works.
  - Infra gotchas worth pre-empting rather than rediscovering: `go-pmtiles` respects `TMPDIR` but SSH sessions often carry a stale one that silently wins over `setdefault()` — force-override and confirm with `df` (cost them a corrupted 310GB archive once); always run `pmtiles verify` after cluster/merge (cheap, catches corruption `show` misses); `rsync` without `--whole-file` balloons memory doing delta-checksums against any pre-existing partial file at the destination; never trust a bare `glob()` to confirm a merge input is complete — use a per-region completion manifest instead (a naive glob once shipped them a silent ~25%-of-country gap); keep leaf-tile and pyramid-tile output in visibly distinct namespaces (a naming collision once cost them 3,344 tiles to a cleanup-script bug).
  - **For discrete classification data specifically** (directly relevant to something like ESA WorldCover's 11 land-cover classes): skip terrarium-style RGB float-packing (that's an elevation-only trick); prefer single-band tiles with the raw class code as the pixel value, recolored client-side via a MapLibre `raster-color`/match expression against the STAC item's `classification:classes` legend — keeps tiles smaller, keeps raw codes queryable, and allows restyling without regenerating tiles. Baking colors into RGB at generation time is the simpler-client alternative but forecloses both of those. Always use **lossless** WebP for class-code data — lossy compression blends/corrupts values at class boundaries. Expect favorable compression versus elevation-tile experience, since large uniform regions (ocean, forest blocks) collapse very well under lossless WebP run-length-style encoding.
  - **A real constraint to design around**: PMTiles/TileJSON support only one global `maxzoom` per archive — there is no per-region maxzoom anywhere in the stack. If parts of a collection only have genuine native detail to a shallower zoom than others, the archive will over-declare depth for those regions and deep-zoom requests there will 204. Mapterhorn is mid-fix on exactly this; the two ways out are generating real (or nearest-neighbor-upsampled) tiles everywhere the archive claims depth, or accepting the honesty gap. Relevant to FERSPAS collections whose native resolution varies by sub-region.
  - On leaving FAO's live WMTS wrapper behind: independently agrees with the instinct from its own experience — "every 'live wrapper as safety net' idea we've had has turned into its own failure mode." No data on the client-side COG+CORS route (option A/B).

**Source Cooperative (`data.source.coop`) Range-request check, run 2026-09-03 in direct response to the `stars-fd` Cloudflare-drops-large-Range finding**: hfu's reaction to that finding was that it would be a "大災害" (major incident) if hit in production — a browser silently attempting a full-file download instead of a ranged tile read — which shifted his read toward "PMTiles generated by us, hosted on Source Cooperative" looking like the sounder default, *provided* Source Cooperative doesn't share the same failure mode. Tested directly rather than assumed: found a real ~102.5GB file already public there (`protomaps/openstreetmap/tiles/v2/protomaps-basemap-opensource-20230408.pmtiles`, 110,147,520,430 bytes — Protomaps' full OSM-derived planet basemap) and issued ranged `GET`s at the start, a ~90GB mid-file offset, and the very end. All three returned correct `206 Partial Content` with accurate `Content-Range`, and `access-control-allow-origin: *` was present throughout (Source Cooperative's own proxy, self-identified as "Source Cooperative Data Proxy v2.3.4", also sits behind Cloudflare — same vendor as `stars.optgeo.org` — but evidently configured differently, since it doesn't reproduce the drop). This is a real, encouraging data point for hosting our own generated PMTiles on Source Cooperative, though it only tests to ~102.5GB, short of the 190–424GB band where `stars-fd` found the failure — not proof the same can't happen higher up, just no evidence of it at a scale in the same order of magnitude. Since our own PMTiles would be zoom-capped and thus almost certainly well under 100GB regardless, this is more reassurance than a hard ceiling concern for option (C) specifically.

**Still needed before a decision**: confirm the identity of the "stars" hosting platform hfu has in mind for option (B) — now identified as `stars.optgeo.org`; asked the `stars-fd` peer session (2026-09-03) for detail on what it is and whether it fits this use case, reply pending as of this writing. Also still needed: a per-collection file-size/zoom-cap feasibility study for option (C) beyond just ESA WorldCover; and a view on whether/how to raise the CORS gap with FAO CSI regardless of which serving option is chosen.

---

### D1 — Repo founded: FERSPAS↔Staccato technical collaboration, split from UNopenGIS/7#932
**Date**: 2026-09-03
**What**: Created `dwg7/ferspas57` (public, CC0 1.0) at hfu's request, made during a `dwg7/staccato-ecosystem`-session Claude Code conversation. Scope: write a `martin catalog`-compatible interface for FERSPAS (FAO's Essential Remote Sensing Data Product Portal, native interface STAC), starting with FERSPAS's orthophoto products, so Staccato's Library can demonstrably reach FERSPAS data. This is the technical follow-through on [`UNopenGIS/7#932`](https://github.com/UNopenGIS/7/issues/932) (DWG5×DWG7 exploration), split out into its own founding issue [`UNopenGIS/7#997`](https://github.com/UNopenGIS/7/issues/997) and its own repo because the work is implementation-shaped (real code, a real conversion pipeline) rather than spec-shaped or value-proposition-shaped. Scaffolded with `README.md`, `CLAUDE.md`, `HANDOVER.md`, `DECISIONS.md`, `LICENSE` (CC0 1.0, copied verbatim from `dwg7/staccato-ecosystem`), matching the established `dwg7` per-project convention (dedicated repo, dedicated session, `HANDOVER.md`/`CLAUDE.md` as onboarding). English throughout, unlike `staccato-ecosystem`'s Japanese-primary content policy, because this repo is explicitly international/FAO-facing.
**Why**: `staccato-spec`'s `catalog-integration.md` §10 already commits to treating STAC as a conversion source rather than Cartographer's primary model; this repo is the first real-world test of that commitment, prompted by DWG 5's June 2026 presentation on FAO/ITU Geo-AI systems identifying FERSPAS as the strongest concrete-collaboration candidate of the three systems surveyed. It also has a real external forcing function: [`UNopenGIS/7#994`](https://github.com/UNopenGIS/7/issues/994), a FAO CSI talk on 2026-09-28 that cites this collaboration directly. Positioned explicitly as a reciprocal, good-faith technical contribution — not a one-sided data pull — in response to FAO CSI's unusually detailed and generous June 2026 technical presentation (see `CLAUDE.md`'s "Positive intent" section for the fuller framing hfu asked to have documented here).

**Knowledge-flow split** (also recorded in `CLAUDE.md`): learnings from this repo are expected to flow to different places depending on their nature — STAC↔martin-catalog technical/architectural patterns to `UNopenGIS/staccato-spec`; collaboration-process/value-proposition learnings to `dwg7/staccato-ecosystem` (this repo is that repo's international-cooperation counterpart to `dwg7/chukei`); generic `dwg7` technical patterns to `dwg7/cafebabe`. Nothing should accumulate here indefinitely without eventually being routed onward.
